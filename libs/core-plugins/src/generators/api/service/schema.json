{
  "$schema": "http://json-schema.org/schema",
  "$id": "ApiModule",
  "cli": "nx",
  "title": "Generate api module",
  "description": "Create api module files",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the api module to generate",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use as the api module?",
      "pattern": "^[a-zA-Z].*$",
      "x-priority": "important"
    },
    "classesName": {
      "type": "string",
      "description": "The name of the module classes to generate",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use as the classes?",
      "pattern": "^[a-zA-Z].*$",
      "x-priority": "important"
    },
    "project": {
      "type": "string",
      "description": "The name of the root project",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What root project would you like to use as the feature?",
      "pattern": "^[a-zA-Z].*$",
      "x-priority": "important"
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
