{
  "$schema": "http://json-schema.org/schema",
  "$id": "MobileFeature",
  "cli": "nx",
  "title": "Generate mobile feature",
  "description": "Create mobile feature files",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the mobile feature to generate",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use as the mobile feature?",
      "pattern": "^[a-zA-Z].*$",
      "x-priority": "important"
    },
    "project": {
      "type": "string",
      "description": "The name of the root mobile project",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What root mobile project would you like to use as the mobile feature?",
      "pattern": "^[a-zA-Z].*$",
      "x-priority": "important"
    }
  },
  "required": ["name"]
}
